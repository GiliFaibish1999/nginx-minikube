apiVersion: v1
kind: Pod
metadata:
  name: nginx-minikube-test-connection
  labels:
    app: nginx-minikube
spec:
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args: ['nginx-minikube:80']
  restartPolicy: Never